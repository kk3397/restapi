


def trian_area(length, width):
    return (length * width) / 2.0


def perm_tri(*args):
    return sum(args)


def rect_area(length, width):
    return length * width


def perm_rec(width, length):
    return (width * 2) + (length * 2)
